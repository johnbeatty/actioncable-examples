<section id="comments" data-channel="comments" data-message-id="<%= message.id %>" data-target="messages.comments">
  <%= render message.comments %>
</section>

<%= render 'comments/new', message: message %>
